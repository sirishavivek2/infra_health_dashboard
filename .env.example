# Copy this file to .env.local and fill in real values.
# .env.local is gitignored — secrets never get committed.

# Local docker-compose Postgres:
DATABASE_URL=postgres://dashboard:dashboard@localhost:5432/infra

# Hosted Postgres (e.g. Supabase) usually needs TLS — then also set:
# DATABASE_SSL=true
